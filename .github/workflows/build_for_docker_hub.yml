name: Docker Build and Push

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-24.04

    env:
      DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USER }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set Docker image tag (with Short SHA)
        run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Login to Docker Hub
        run: echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USER" --password-stdin

      - name: Build Docker image
        run: |
          docker build -t ndhakara/kartopu:${{ env.IMAGE_TAG }} -t ndhakara/kartopu:latest .

      - name: Push Docker image
        run: |
          docker push ndhakara/kartopu:${{ env.IMAGE_TAG }}
          docker push ndhakara/kartopu:latest
